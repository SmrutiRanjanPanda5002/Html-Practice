let arr=[1,2,3,4,5,6,7]
arr[0]=1234//We can do this only in array.
console.log(arr)
console.log(arr.length)
console.log(arr[0]) 
//String is immutable
//Array is muteble
// console.log(arr.toString())
// Array.pop();
// Array.push(we can write any thing inside it)
// Array.shift() //Brother of pop
// Array.unshift() //brother of push
//Array.sort();
//splice(index,to the index);
//Array.revers()